# Combined dependencies for running the entire AIoT stack locally (without Docker).
# Individual per-component requirements.txt files exist under each service folder
# and are the authoritative source for container builds.

# ---- simulator + ingestion + publisher ----
numpy>=1.24,<2
pandas>=2.0
paho-mqtt>=1.6,<2
python-dotenv>=1.0

# ---- ml ----
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7
seaborn>=0.12

# ---- dashboard ----
streamlit>=1.28
plotly>=5.17
